const apiData = {
    name: '{NAME}',
    description: '{DESCRIPTION}'
};

const mappedData = {
    name: '{NAME}',
    description: '{DESCRIPTION}'
};

module.exports = {
    paymentMethod: {
        apiData,
        mappedData
    }
};
