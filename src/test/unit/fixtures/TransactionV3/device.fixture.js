const apiData = {
    id: '{ID}',
    ipAddress: '{IP_ADDRESS}'
};

const mappedData = {
    id: '{ID}',
    ipAddress: '{IP_ADDRESS}'
};

module.exports = {
    device: {
        apiData,
        mappedData
    }
};
